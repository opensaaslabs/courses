# Reference Model: Credential Graph

```text
Learner -> submits -> Evidence
Evidence -> reviewedBy -> Reviewer
Evidence -> supports -> Badge
Badge -> contributesTo -> Certification
Credential -> issuedTo -> Holder
Credential -> issuedBy -> Issuer
Verifier -> verifies -> Credential
```

Credentials must be evidence-backed and verification-ready.
