# Golden Outputs

Golden outputs are canonical expected outputs for conformance fixtures.

Implementations do not need byte-identical formatting, but they must preserve the same semantics, stable IDs, node identities, relation semantics, and required fields.
